import { ParallaxBgDirective } from './parallax-bg.directive';

describe('ParallaxBgDirective', () => {
  it('should create an instance', () => {
    const directive = new ParallaxBgDirective();
    expect(directive).toBeTruthy();
  });
});
